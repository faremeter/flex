/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FLEX_PROGRAM_ADDRESS } from "../programs";

export const VOID_PENDING_DISCRIMINATOR = new Uint8Array([
  24, 204, 242, 87, 156, 12, 214, 62,
]);

export function getVoidPendingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VOID_PENDING_DISCRIMINATOR,
  );
}

export type VoidPendingInstruction<
  TProgram extends string = typeof FLEX_PROGRAM_ADDRESS,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountPending extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPending extends string
        ? WritableAccount<TAccountPending>
        : TAccountPending,
      ...TRemainingAccounts,
    ]
  >;

export type VoidPendingInstructionData = { discriminator: ReadonlyUint8Array };

export type VoidPendingInstructionDataArgs = {};

export function getVoidPendingInstructionDataEncoder(): FixedSizeEncoder<VoidPendingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: VOID_PENDING_DISCRIMINATOR }),
  );
}

export function getVoidPendingInstructionDataDecoder(): FixedSizeDecoder<VoidPendingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getVoidPendingInstructionDataCodec(): FixedSizeCodec<
  VoidPendingInstructionDataArgs,
  VoidPendingInstructionData
> {
  return combineCodec(
    getVoidPendingInstructionDataEncoder(),
    getVoidPendingInstructionDataDecoder(),
  );
}

export type VoidPendingInput<
  TAccountEscrow extends string = string,
  TAccountOwner extends string = string,
  TAccountPending extends string = string,
> = {
  escrow: Address<TAccountEscrow>;
  owner: TransactionSigner<TAccountOwner>;
  pending: Address<TAccountPending>;
};

export function getVoidPendingInstruction<
  TAccountEscrow extends string,
  TAccountOwner extends string,
  TAccountPending extends string,
  TProgramAddress extends Address = typeof FLEX_PROGRAM_ADDRESS,
>(
  input: VoidPendingInput<TAccountEscrow, TAccountOwner, TAccountPending>,
  config?: { programAddress?: TProgramAddress },
): VoidPendingInstruction<
  TProgramAddress,
  TAccountEscrow,
  TAccountOwner,
  TAccountPending
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    escrow: { value: input.escrow ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    pending: { value: input.pending ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("escrow", accounts.escrow),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("pending", accounts.pending),
    ],
    data: getVoidPendingInstructionDataEncoder().encode({}),
    programAddress,
  } as VoidPendingInstruction<
    TProgramAddress,
    TAccountEscrow,
    TAccountOwner,
    TAccountPending
  >);
}

export type ParsedVoidPendingInstruction<
  TProgram extends string = typeof FLEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    escrow: TAccountMetas[0];
    owner: TAccountMetas[1];
    pending: TAccountMetas[2];
  };
  data: VoidPendingInstructionData;
};

export function parseVoidPendingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVoidPendingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      escrow: getNextAccount(),
      owner: getNextAccount(),
      pending: getNextAccount(),
    },
    data: getVoidPendingInstructionDataDecoder().decode(instruction.data),
  };
}
