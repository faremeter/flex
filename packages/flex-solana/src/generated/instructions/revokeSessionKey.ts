/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FLEX_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_SESSION_KEY_DISCRIMINATOR = new Uint8Array([
  81, 192, 32, 110, 104, 116, 144, 151,
]);

export function getRevokeSessionKeyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_SESSION_KEY_DISCRIMINATOR,
  );
}

export type RevokeSessionKeyInstruction<
  TProgram extends string = typeof FLEX_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountSessionKeyAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountEscrow extends string
        ? ReadonlyAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountSessionKeyAccount extends string
        ? WritableAccount<TAccountSessionKeyAccount>
        : TAccountSessionKeyAccount,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeSessionKeyInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokeSessionKeyInstructionDataArgs = {};

export function getRevokeSessionKeyInstructionDataEncoder(): FixedSizeEncoder<RevokeSessionKeyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REVOKE_SESSION_KEY_DISCRIMINATOR }),
  );
}

export function getRevokeSessionKeyInstructionDataDecoder(): FixedSizeDecoder<RevokeSessionKeyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeSessionKeyInstructionDataCodec(): FixedSizeCodec<
  RevokeSessionKeyInstructionDataArgs,
  RevokeSessionKeyInstructionData
> {
  return combineCodec(
    getRevokeSessionKeyInstructionDataEncoder(),
    getRevokeSessionKeyInstructionDataDecoder(),
  );
}

export type RevokeSessionKeyInput<
  TAccountOwner extends string = string,
  TAccountEscrow extends string = string,
  TAccountSessionKeyAccount extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  escrow: Address<TAccountEscrow>;
  sessionKeyAccount: Address<TAccountSessionKeyAccount>;
};

export function getRevokeSessionKeyInstruction<
  TAccountOwner extends string,
  TAccountEscrow extends string,
  TAccountSessionKeyAccount extends string,
  TProgramAddress extends Address = typeof FLEX_PROGRAM_ADDRESS,
>(
  input: RevokeSessionKeyInput<
    TAccountOwner,
    TAccountEscrow,
    TAccountSessionKeyAccount
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeSessionKeyInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountEscrow,
  TAccountSessionKeyAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: false },
    sessionKeyAccount: {
      value: input.sessionKeyAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("escrow", accounts.escrow),
      getAccountMeta("sessionKeyAccount", accounts.sessionKeyAccount),
    ],
    data: getRevokeSessionKeyInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeSessionKeyInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountEscrow,
    TAccountSessionKeyAccount
  >);
}

export type ParsedRevokeSessionKeyInstruction<
  TProgram extends string = typeof FLEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    escrow: TAccountMetas[1];
    sessionKeyAccount: TAccountMetas[2];
  };
  data: RevokeSessionKeyInstructionData;
};

export function parseRevokeSessionKeyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeSessionKeyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      escrow: getNextAccount(),
      sessionKeyAccount: getNextAccount(),
    },
    data: getRevokeSessionKeyInstructionDataDecoder().decode(instruction.data),
  };
}
