/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FLEX_PROGRAM_ADDRESS } from "../programs";

export const EMERGENCY_CLOSE_DISCRIMINATOR = new Uint8Array([
  72, 5, 66, 138, 108, 194, 37, 175,
]);

export function getEmergencyCloseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EMERGENCY_CLOSE_DISCRIMINATOR,
  );
}

export type EmergencyCloseInstruction<
  TProgram extends string = typeof FLEX_PROGRAM_ADDRESS,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type EmergencyCloseInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type EmergencyCloseInstructionDataArgs = {};

export function getEmergencyCloseInstructionDataEncoder(): FixedSizeEncoder<EmergencyCloseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EMERGENCY_CLOSE_DISCRIMINATOR }),
  );
}

export function getEmergencyCloseInstructionDataDecoder(): FixedSizeDecoder<EmergencyCloseInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getEmergencyCloseInstructionDataCodec(): FixedSizeCodec<
  EmergencyCloseInstructionDataArgs,
  EmergencyCloseInstructionData
> {
  return combineCodec(
    getEmergencyCloseInstructionDataEncoder(),
    getEmergencyCloseInstructionDataDecoder(),
  );
}

export type EmergencyCloseInput<
  TAccountEscrow extends string = string,
  TAccountOwner extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  escrow: Address<TAccountEscrow>;
  owner: TransactionSigner<TAccountOwner>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getEmergencyCloseInstruction<
  TAccountEscrow extends string,
  TAccountOwner extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof FLEX_PROGRAM_ADDRESS,
>(
  input: EmergencyCloseInput<
    TAccountEscrow,
    TAccountOwner,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): EmergencyCloseInstruction<
  TProgramAddress,
  TAccountEscrow,
  TAccountOwner,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    escrow: { value: input.escrow ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("escrow", accounts.escrow),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getEmergencyCloseInstructionDataEncoder().encode({}),
    programAddress,
  } as EmergencyCloseInstruction<
    TProgramAddress,
    TAccountEscrow,
    TAccountOwner,
    TAccountTokenProgram
  >);
}

export type ParsedEmergencyCloseInstruction<
  TProgram extends string = typeof FLEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    escrow: TAccountMetas[0];
    owner: TAccountMetas[1];
    tokenProgram: TAccountMetas[2];
  };
  data: EmergencyCloseInstructionData;
};

export function parseEmergencyCloseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEmergencyCloseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      escrow: getNextAccount(),
      owner: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getEmergencyCloseInstructionDataDecoder().decode(instruction.data),
  };
}
